<div class="container-fluid mt-2">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Writters's Blog</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="posts/new">Create new post</a>
          </li>
          <li class="nav-item">
            <% if !user_signed_in? %>
             <%= link_to 'Log In', new_user_session_path, class: "btn btn-dark"%>
            <% end %>
          </li>
          <li class="nav-item">
            <% if user_signed_in? %>
             <%= link_to 'Login out', destroy_user_session_path, method: :delete, class: "btn btn-dark"%>
            <% end %>
          </li>
        </ul>
      </div>
    </div>
  </nav>
</div>
